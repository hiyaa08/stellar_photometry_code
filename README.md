The Python library STC.py is equipped to perform stellar photometry given an image of two stars. It can also calculate the temperature of these stars. 

The library was initially only written for imaged taken by our CMOS camera, the STC-7 and could only handle .fits files. However, it was later modified to handle images taken by a DSLR camera and now can be used with images of .JPEG type as well. 
